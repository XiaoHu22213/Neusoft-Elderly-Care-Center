<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neuedu.mapper.CheckInMapper">

<select id="list" resultType="CheckIn">
    select id,
    customername,
    customersex,
    customerage,
    idcard,
    roomid,
    buildingid,
    recordid,
    checkindate,
    expirationdate,
    contacttel,
    eldertype,
    remarks,
    checkoutdate,
    checkouttype,
    checkoutreason,
    status,
    asktime,
    auditopinion,
    auditperson,
    audittime,
    checkoutremarks,
    from check_in
</select>
</mapper>